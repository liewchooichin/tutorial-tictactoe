import { createContext } from "react";

/**For input text */
export const TextContext = createContext(null);
export const TextContextDispatch = createContext(null);

/**Status of sending */
export const IsSendingContext = createContext(null);
export const IsSendingContextDispatch = createContext(null);